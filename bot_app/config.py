"""Константы и глобальное состояние бота."""

import os

# Читаем токен из переменных окружения
TOKEN = os.environ.get("BOT_TOKEN")

if not TOKEN:
    raise ValueError(
        "Переменная окружения BOT_TOKEN не задана. "
        "Задайте её перед запуском, например: export BOT_TOKEN=ваш_токен"
    )

# Временный пароль, потом можно вынести в config
ADMIN_PASSWORD = "1234"

# Названия смен (короткие)
shift_names = {1: "Первая", 2: "Вторая", 3: "Третья"}

# Названия смен с часами (используются в нескольких экранах)
shift_names_with_clock = {
    1: "1️⃣ Первая смена (09:00-17:00)",
    2: "2️⃣ Вторая смена (17:00-01:00)",
    3: "3️⃣ Третья смена (01:00-09:00)"
}

# Глобальное хранилище ожидаемых пользователей
pending_users = {}  # {username_lower: user_data}
